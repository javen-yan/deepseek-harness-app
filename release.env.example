# Source this file in a shell before running a release build.
# The private key may be provided as a file path or inline key content.

export TAURI_SIGNING_PRIVATE_KEY="$HOME/.tauri/deepseek-harness-app.key"
export TAURI_SIGNING_PRIVATE_KEY_PASSWORD=""
